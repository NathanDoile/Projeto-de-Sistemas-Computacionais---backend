spring:
  datasource:
    url: jdbc:mysql://localhost:3306/bd_projeto
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: validate

  mail:
    host: smtp.gmail.com
    port: 587
    username: projetodesistemas3a2026@gmail.com
    password: backend3a2026
    protocol: smtp
    properties:
      "[mail.smtp.auth]": true
      "[mail.smtp.starttls.enable]": true
      "[mail.smtp.starttls.required]": true
      "[mail.smtp.ssl.trust]": smtp.gmail.com

gemini:
  baseurl: https://generativelanguage.googleapis.com/v1beta/models/gemini-3-flash-preview:generateContent?key=%s
  api:
    key: CHAVEAPI