spring:
  datasource:
    url: jdbc:mysql://localhost:3306/bd_projeto
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: validate

  mail:
    host: smtp.gmail.com
    port: 587
    username: projetodesistemas3a2026@gmail.com
    password: edzx uhrd ypyz ihre
    protocol: smtp
    properties:
      "[mail.smtp.auth]": true
      "[mail.smtp.starttls.enable]": true
      "[mail.smtp.starttls.required]": true
      "[mail.smtp.ssl.trust]": smtp.gmail.com

gemini:
  baseurl: https://generativelanguage.googleapis.com/v1beta/models/gemini-3-flash-preview:generateContent?key=%s
  api:
    key: secrets.CHAVE_API_GEMINI

logging:
  pattern:
    console: "%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
    org.springframework.web.servlet: DEBUG
    org.springframework.security: DEBUG